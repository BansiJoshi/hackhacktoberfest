#include <iostream>

using namespace std;

int main()
{
    int n,i;

    cout<<"Enter the range to find sum:"
    cin >> n;

    int sum;

    for(i=0; i<=n; i++)
    {
            if(i%2==0)
            {
                sum += i;
            }
    }
    cout<<sum
    return 0;
}
